using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace MyGameplay.Signal
{
	/*public class SignalState
	{
		public bool signalOn;
		public bool signal2On;

		public SignalState()
		{
			//SetState();
			SetState(false, false);
		}
		public SignalState(bool signalOn)
		{
			//SetState(signalOn);
			SetState(signalOn, false);
		}
		public SignalState(bool signalOn, bool signal2On)
		{
			SetState(signalOn, signal2On);
		}

		public void SetState(bool signalOn)
		{
			this.signalOn = signalOn;
		}
		public void SetState(bool signalOn, bool signal2On)
		{
			this.signalOn = signalOn;
			this.signal2On = signal2On;
		}
		public void SetState2(bool signal2On)
		{
			this.signal2On = signal2On;
		}

		public override string ToString()
		{
			return (signalOn ? "true" : "false") + "," + (signal2On ? "true" : "false");
		}
	}*/
}
