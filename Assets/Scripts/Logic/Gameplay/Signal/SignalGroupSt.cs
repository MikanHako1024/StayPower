using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace MyGameplay.Signal
{
	/*public struct SignalGroupSt
	{
		public int groupId;
		public bool hasGroup;
		public int group2Id;
		public bool hasGroup2;

		public SignalGroupSt(int groupId)
		{
			this.groupId = groupId;
			hasGroup = true;
			group2Id = 0;
			hasGroup2 = false;
		}

		public SignalGroupSt(int groupId, int gourp2Id)
		{
			this.groupId = groupId;
			hasGroup = true;
			this.group2Id = gourp2Id;
			hasGroup2 = true;
		}

		public static SignalGroupSt empty = new SignalGroupSt();

		public override string ToString()
		{
			return (hasGroup ? "true" : "false") + " " + groupId + " " + (hasGroup2 ? "true" : "false") + " " + group2Id;
		}
	}*/
}
